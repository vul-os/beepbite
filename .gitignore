node_modules

.venv

dist
dev-dist

billingmodel/results/
billingmodel/__pycache__/

.firebase

# Env files — commit only the .example siblings
.env
.env.dev
.env.main
.env.local
.env.*.local

# Go build artifacts
backend/bin/
backend/tmp/
backend/beepbite-server-local
backend/beepbite-server-*

# Local workspace for the -tags patala build (see backend/Makefile's go.work
# target). Wires the sibling patala-go module in for local cgo dev/test only
# — never committed, and never needed by the default backend build.
backend/go.work
backend/go.work.sum

.vscode

playwright-report
# Playwright test artifacts
test-results/

# agent tooling
.agents/
.claude/
